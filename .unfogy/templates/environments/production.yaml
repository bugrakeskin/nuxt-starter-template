# Template only. Provisioning fills the MWO identity, domains, and non-secret
# provider references. Placement intent remains in the Control Plane record.
schema_version: 1
kind: CustomerMWOEnvironment
mwo_ref: __MWO_REF__
environment: production
recipe:
  name: nuxt-supabase
  version: v1
domains: {}
required_environment_keys:
  - NUXT_PUBLIC_APP_NAME
  - NUXT_PUBLIC_SUPABASE_URL
  - NUXT_PUBLIC_SUPABASE_KEY
  - SUPABASE_DB_URL
